// Idle state now has no visual of its own — BensonSeal (rendered on top by
// BensonMainScreen) is the only seal graphic on screen. This used to draw a second,
// overlapping "B" panel underneath it.
export function IdleCard(_props: { pulsing: boolean }) {
  return null;
}
