# BENSON Recovery Matrix — 2026-09-13 (offline, no device)

Branch: `benson_recovery_2026_09_13`
Anchor: `f002639946f4807b959b17d2f70b151859beefbf` (recovery anchor only, NOT a DEVICE_PASS baseline)

No row below is DEVICE_PASS. Every row is IMPLEMENTED_ONLY or BLOCKED until tomorrow's real-device test (`BENSON_DEVICE_RECOVERY_TEST_2026-09-14.md`) proves it.

| LAYER | CAPABILITY | KNOWN_PREVIOUS_DEVICE_EVIDENCE | CURRENT_CODE_STATUS | FILES_CHANGED | STATIC_TESTS | DEVICE_TEST_REQUIRED | STATUS |
|---|---|---|---|---|---|---|---|
| 1 | Core service / mic ownership | Process alive, foreground service alive, accessibility bound, survives backgrounding — confirmed via `adb` this session (no device change needed) | Reviewed only, no code change. `setMicOwner()` is the single mutation point; native, epoch-guarded 45s watchdog on COMMAND_STT/TTS/CONFIRMATION_STT; `armNativeWake()` explicitly refuses to arm while mic is owned by COMMAND_STT/TTS/CALL/CONFIRMATION_STT. This guard is correct and is what makes Layer 2's fix safe — no contradiction found. | none | tsc n/a (no change); confirmed via `adb shell dumpsys`/`ps` earlier this session | Re-run Layer 1 device checks tomorrow as the regression gate before anything else | IMPLEMENTED_ONLY (reviewed, unchanged) |
| 2 | Generic confirmation / reply listening | Device-log-proven FAIL: "deschide calculatorul" → "O deschid?" → local-Whisper self-echo garbled the reply (attempt 1); COMMAND_STT captured 0 bytes and released to WAKE, which captured a clean "Da, da deschide." and discarded it as WAKE_NO_MATCH (attempt 2) | Fixed: `finishHandledMission()` sets a one-shot `pendingDisambigReplyRef` flag for a disambiguation result; `endTtsBlock()` arms the SAME native confirmation listener already proven for WhatsApp's mission-gate case. The pre-existing `pendingConfirmationIdRef` guard inside `doStartListening()` automatically stops the generic mic loop from competing. Second, independent bug found and fixed: `matchDisambiguationPick()` never recognized a plain "da" for a single-candidate proposal (only ordinal/name-token matches) — a clean "da" would still have failed to open Calculator even with correct mic routing. Fixed with a narrow single-candidate-plus-yes-pattern branch. | `app/index.tsx`, `src/core/orchestrator/missionOrchestrator.ts` | tsc: 0 errors. `gradlew assembleRelease`: BUILD SUCCESSFUL | TEST A1 (3x), A2, A3 — see test script | IMPLEMENTED_ONLY |
| 3 | Calculator / generic app governance (OPEN_APP → resolve → disambiguate → confirm → execute) | Opened once, then failed on repeat, in the same session that surfaced Layer 2's bug | No change needed beyond Layer 2 — the OPEN_APP/disambiguation dispatch itself (`governAction` call in `missionOrchestrator.ts`'s disambiguation-consume block) was already correct; the defect was entirely upstream in the reply-capture layer. Reviewed, not touched. | none (inherits Layer 2's fix) | none new | TEST A1 must actually open Calculator; verify typing still works if BENSON types into it | IMPLEMENTED_ONLY (depends entirely on Layer 2) |
| 4 | Contact resolution | "Hana"/"Hannah" produced unrelated candidates ("Peter Pane Johannes Gunzel") via the disabled PERSON_RESOLVE path, before it was gated off this session | PERSON_RESOLVE (`contextResolver.ts`) stays disabled at its `app/index.tsx` call site (explicit prior decision, unchanged tonight). Added unit tests for the LIVE resolver (`contactResolver.ts`, tiered exact→alias→partial→fuzzy) actually used by `whatsappTool.ts` today: Hannah/Baby exact, Hana fuzzy-rescue, exact-never-overridden-by-fuzzy, real ambiguity vs. false ambiguity, alias match, missing-phone. | `src/core/contacts/contactResolver.test.ts` (new) | tsc: 0 errors (file excluded from main tsconfig, same as the existing contextResolver.test.ts). **Could not execute via `node --test`** — `contactResolver.ts` imports `logAudioDiag` from `benson-foreground-service` (added earlier this session for diagnostic logging), which pulls in an Expo native-module chain Node's `--experimental-strip-types` cannot process. Verified by manual trace against the resolver's tier logic instead. | Confirm "Hannah"/"Hana"/"Baby" resolve as expected against the REAL address book (test fixtures are synthetic) | IMPLEMENTED_ONLY (tests written, not machine-run) |
| 5 | WhatsApp CALL | Native call lifecycle (`runWhatsAppCallNative`/`runWhatsAppOpenConversationCall`, IDLE→CALL_VERIFIED_ACTIVE→ending) previously reported working; this session's attempts were blocked by `contactResolver.ts` returning `status=ambiguous` for "Hana"/"Hanna", not a call-lifecycle defect | Not touched tonight — preserved as-is per explicit instruction. The additive `verifiedHeaderText`/`nameMatch` fields (added earlier this session for the identity bridge) are still present, defaulted, only active on an already-successful path; re-read this session, found no mechanism by which they could cause the observed `ambiguous` blocker. | none | tsc/build already cover this (no isolated test) | Baby CALL, then Hannah CALL, full lifecycle + post-call recovery | IMPLEMENTED_ONLY (unchanged; blocker is Layer 4/resolver behavior on real contacts, not this layer) |
| 6 | WhatsApp direct WRITE (one-shot, "scrie-i lui X că ...") | Not isolated from Layer 7 in this session's tests | Shares Phase A (`maybeRunWhatsAppWritePhaseA` → `prepareMessageDirect`) with Layer 7; no separate defect found or fixed. Not touched tonight. | none | covered by tsc/build | "scrie-i lui Baby că ajung la opt" → type → "Îl trimit?" → STOP (no send) | IMPLEMENTED_ONLY (unchanged) |
| 7 | WhatsApp missing-message-body continuation | Device-log-proven FAIL this session: reply to "Ce să-i scriu?" reached `runTool()`'s hard `prepareMessage` refusal ("Scrierea de mesaje... dezactivată momentan"), because `resumePendingTask()` hardcoded `confirmed=true` on every resume, skipping Phase A entirely | Fixed earlier this session (present in anchor `f002639`): `resumePendingTask()` now passes `confirmed=false` specifically when resuming a message-body injection, so Phase A runs for real with the now-known text. Re-verified by re-reading the diff tonight — still correct, not regressed by tonight's Layer 2/9 edits to the same function. | none tonight (fix already in anchor) | tsc/build (already passed when originally fixed) | Full "vreau să trimit un mesaj lui Hannah" → "Ce mesaj?" → reply → open/verify/type → "Îl trimit?" — never actually confirmed on device (test was redirected to CALL, then Calculator, before retest) | IMPLEMENTED_ONLY (fix present, never device-confirmed) |
| 8 | YES/NO confirmation + SEND (Phase B) | Native one-shot confirmation listener (`NativeConfirmationListener`) previously completed one full round trip on device (WA_WRITE_SEND_ENTER → SEND_CLICK ok=true → SENT_VERIFIED), per this session's earlier work | Unchanged tonight. Idempotency (`SEND_ATTEMPTED` persisted before tap, re-entry re-verifies never re-presses) reviewed, looks intact. | none | covered by tsc/build | 5x send-once-only with Baby, NO/cancel behavior, no duplicate send | IMPLEMENTED_ONLY (unchanged; previously partially device-proven, needs a fresh full run) |
| 9 | Correction continuity ("nu Baby, Hannah") | **Did not exist before tonight** — audit found zero handling for this anywhere in the codebase | New, minimal: `app/index.tsx`'s pending-task gate detects a correction only when the task already typed a message and is awaiting send (`waWriteMissionId` set), narrow regex ("nu X, Y" / "am spus Y" / "mă refer la Y"); `resumePendingTask()` gets a new `injectedContactCorrection` param that swaps the contact, voids the stale Phase A result, and forces a fresh Phase A run for the new contact with the same message. | `app/index.tsx`, `src/core/orchestrator/missionOrchestrator.ts` | tsc: 0 errors. `gradlew assembleRelease`: BUILD SUCCESSFUL | Full scenario, both directions ("nu Baby, Hannah" and "nu Hannah, Baby"), plus confirm a plain "nu" alone still just cancels | IMPLEMENTED_ONLY — **highest risk item tonight, brand new, zero device history** |
| 10 | Wake native→JS handoff | Native detection proven (`WAKE_STT_RESULT`/`WAKE_MATCH_EXACT` observed live); JS-side consumption while suspended was an open, explicitly paused investigation from earlier this session | **Not touched tonight**, per repeated explicit instruction across this session ("do not touch wake") and tonight's brief ("remains IMPLEMENTED_ONLY tonight" / inspect-only unless a concrete contradiction is found — none was newly found). Existing mechanism: `pendingWakeCommand` (always set on hotword detect) + `takePendingWakeCommand()` atomic read/clear + a dedicated 5s `WakeHandoffWatchdog`, consumed via both the live event listener and a heartbeat-poll fallback. | none | none new | Screen-off wake test (last in tomorrow's script, lowest priority) | IMPLEMENTED_ONLY (pre-existing, unmodified; known open gap, not resolved) |

## Explicitly not touched tonight
YouTube governance, Spotify governance, MediaSession core, shopping governance, Amazon/TV/laptop/watch/website integrations, wake-word detection algorithm, visual/UI design, dependency/package versions, unrelated formatting/cleanup.

## Commit chain (branch `benson_recovery_2026_09_13`, from anchor `f002639`)
1. `9269ec7` — RECOVERY_L2_GENERIC_CONFIRMATION_IMPLEMENTED
2. `b97560c` — RECOVERY_L4_CONTACT_RESOLUTION_IMPLEMENTED
3. `61da163` — RECOVERY_L9_CORRECTION_CONTINUITY_IMPLEMENTED

Layers 1, 3, 5, 6, 7, 8, 10 required no new commit — reviewed/verified only, or already fixed earlier this session and present unmodified in the anchor.
